# 潘庄小学打字练习乐园 — 更新进度日志

> 本文件自动记录每次网站更新的任务进度，按日期倒序排列。

---

## 2026-07-31（第四轮：触屏练习只保留英文 + 横屏优化）

### ✅ 触屏练习只保留英文练习
- 去掉「拼音打字」（中文）练习，只保留「字母定位」和「英文单词」两个英文练习

### ✅ 横屏时机调整 + 可切回竖屏
- 点击「触屏练习」进入选模式界面时**不横屏**（保持竖屏选择模式/关卡）
- 学生真正进入练习界面（`touch-play-screen`）后才自动切横屏
- 练习界面顶部新增「📱 竖屏」按钮，随时可切回竖屏查看；再点「🔄 横屏」可切回横屏（电脑上自动隐藏该按钮）

### ✅ 横屏自适应手机/平板尺寸
- 横屏后练习界面铺满整个横屏画布（`100vh × 100vw`）
- 键盘区改为弹性布局：键盘根据剩余空间自动缩放（`min()` 限制大小），手机/平板都能把标题、单词/字母目标区、键盘、空格键完整显示出来
- 目标字母/单词、中文意思、进度等字号随屏幕自动适配，不溢出不截断

## 2026-07-31（第三轮：手机/平板适配）

### ✅ 触屏练习自动横屏（手机/平板）
- 进入触屏练习（选模式或开始练习）后自动切换横屏显示，适配手机和平板界面
- 优先调用系统横屏锁定 `screen.orientation.lock('landscape')`；在微信/QQ等不支持锁定的浏览器里，自动用 CSS 旋转（`body.landscape-rotated`）把竖屏页面旋转为横屏效果
- 退出触屏练习返回首页时自动恢复竖屏；手机物理旋转方向变化时自动适配

### ✅ 微信/QQ 里自动朗读单词（重点修复）
- **根因**：iPhone 的微信/QQ 内置浏览器（WKWebView）不支持网页 `speechSynthesis` 语音合成，所以单词一直不发声
- **修复**：改用「有道词典英音 MP3 音频」播放读音（`dict.youdao.com` 英音接口，国内可直接访问），普通浏览器、微信、QQ、手机、平板全部支持
- 首读在点击「开始」的用户手势内同步触发，规避微信/QQ 的自动播放限制；播放失败时自动回退到浏览器自带语音合成
- 依然保持：单词出现立即朗读 → 每 2 秒重读一遍 → 输完该单词停止并进入下一个；🔊 按钮随时重读，朗读时按钮高亮闪烁
- 修复 🔊 按钮缺少 `id` 导致朗读状态高亮不生效的小问题

## 2026-07-31（第二轮修复）

### ✅ 彻底修复键盘输入问题（中文 + 英文字母）
- **根因**：电脑上中文输入法（拼音）处于激活状态时，浏览器会把字母按键交给输入法处理（`keyCode=229`/`Process`），网页根本收不到字母，所以 a/d/x/w/z/q/s 等字母无反应或弹出输入法候选框（看起来像「修改速度」提示）
- **修复1**：检测到中文输入法组合时弹出提示「请按 Shift 切换到英文输入法」，提醒学生/老师切换
- **修复2**：英文模式直接拦截并处理按键（`preventDefault`+`stopPropagation`），不受输入法干扰
- **修复3**：中文输入增强——用 `compositionupdate` 捕获上屏文字（兼容 `compositionend.data` 为空的老浏览器），短文练习可正常用拼音输入汉字，支持一次上屏多个字
- **修复4**：所有非 ASCII 字符（含中文弯引号""、全角标点）都交给输入法处理，不再被误拦截

### ✅ 意见反馈入口更明显
- 学生首页保留「💬 意见反馈」卡片
- 顶部蓝色栏新增「💬 意见反馈」按钮，登录后随时可见、随时提交

### ✅ 英文单词练习全面升级（人教版三/四/五年级）
- **新增年级选择**：开始前先选 🌱三年级 / 🌿四年级 / 🌳五年级（对应人教版小学英语教材），每个年级 6 关、每关 10 词，共 180 个单词
- 三年级：文具问候、颜色、身体、动物、数字、食物
- 四年级：教室、书包、朋友家庭、我的家、晚餐、职业
- 五年级：星期、课程活动、自然、四季天气、衣物、运动
- **显示中文意思**：单词下方同时显示「中文意思：xxx」
- **英式朗读**：单词出现后自动用英式发音朗读，每 2 秒重复一次，直到学生输入完该单词；可点 🔊 按钮随时重听；退出练习自动停止朗读
- **朗读可靠性修复**：修复 Chrome 里「取消后立即朗读」会被静默丢弃的浏览器 bug（改为不打断的循环重读 + 首读延迟 100ms + 🔊 按钮强制重读），朗读时 🔊 按钮会高亮闪烁提示正在读

## 2026-07-31（第一轮修复）

### ✅ 修复英文练习键盘输入bug
- **触屏练习（含英文单词模式）新增电脑键盘支持**：用实体键盘输入字母、⌫退格、空格/回车确认均可正常工作
- 拦截按键默认行为（`preventDefault` + `stopPropagation`），避免浏览器/插件弹出「修改速度」等干扰提示，输入法组合中的按键不被误拦截

### ✅ 修复短文练习无法输入中文
- **根因**：原代码拦截所有 `keydown` 事件，中文输入法（拼音）的按键被 `preventDefault` 拦截，无法输入
- **修复**：识别中文内容后不再拦截键盘事件，交给输入法处理；新增 `compositionend` 监听，输入法上屏的中文与课文逐字匹配（支持一次输入多个字），错误标红、正确前进
- 中文提示区显示「用拼音输入此汉字」

### ✅ 句子练习内容改为人教版三四年级课文
- 简单（三年级）：Hello! I'm Miss White. What's your name? 等打招呼/自我介绍句子
- 中等（四年级上）：My classroom / My schoolbag / My friends / My home / Dinner's ready / Meet my family 等单元句子
- 困难（四年级下）：What time is it? / Weather / At the farm / My clothes / Shopping 等单元句子
- 每级12句随机抽取，练习前设置中可选三年级/四年级/五年级难度

### ✅ 学生端新增「意见反馈」功能
- 学生首页新增「💬 意见反馈」入口
- 可选择反馈类型：🐞遇到问题 / 💡修改意见 / 🚀新功能建议 / 📝其他
- 自动带上班级和姓名，提交后保存到本地 + 上传云端

### ✅ 教师端新增意见反馈查看
- 教师后台新增「💬 意见反馈」Tab，查看所有学生反馈（时间/班级/姓名/类型/内容/状态）
- 支持「标记已处理」和刷新按钮

### ⚠️ 需要老师操作：创建云端反馈表
- 学生反馈要同步到云端，需在 Supabase 后台 SQL 编辑器执行：
```sql
create table if not exists feedbacks (
  id serial primary key,
  created_at timestamptz default now(),
  class text,
  name text,
  category text,
  content text,
  status text default 'pending'
);
```
- 未创建前，反馈只保存在本机，教师端看不到，但不影响其他功能

### ✅（当日较早）成绩上传、教师后台升级、本班排行、英文单词模式等

### ✅ 学生在家使用 - 成绩自动上传云端
- 练习完成后成绩自动写入 Supabase 云端，教师可从任何设备查看
- **新增离线补传机制**：网络异常时成绩先存入本地待同步队列（`tpPendingGrades`），联网后下次打开页面自动补传，成绩不丢失

### ✅ 教师后台成绩查看全面升级
- 新增 **🔄 刷新云端数据** 按钮，一键从 Supabase 拉取最新成绩
- 新增 **📝 练习记录** 视图：按日期范围、班级、姓名、练习类型筛选所有学生的每条练习记录
- 成绩汇总表新增「最近练习」列，默认按最近练习时间排序，教师一眼看出谁最近练习了
- 学生成绩详情支持日期范围 + 练习类型筛选
- 新增练习记录 CSV 下载（按当前筛选条件导出）

### ✅ 学生端新增最近练习记录
- 「我的成绩」页面新增「最近练习记录」表格，展示本人最近10次练习

### ✅ 学生端新增本班成绩排行
- 「我的成绩」页面新增「👥 本班成绩排行」表格，仅显示本班同学
- 按平均速度排名（并列时看最高速度/练习次数），展示练习次数、平均速度、平均正确率、最高速度
- 本人所在行高亮显示并标注「(我)」，前3名显示 🥇🥈🥉

### ✅ 触屏练习新增英文单词模式
- 新增第3种触屏模式「🔤 英文单词」：10关 × 每关10个单词，小学三年级水平
- 关卡主题：动物世界、森林动物、美味水果、食物饮料、七彩颜色、学习文具、我的身体、幸福一家、数字天地、快乐校园
- 练习前弹出关卡选择界面，可随时「取消」退出；练习中可通过「✕ 退出」随时返回
- 单词逐字母点击输入，正确字母变绿、错误变红扣分，输完整词 +20 分自动进入下一个
- 支持 ⌫ 退格修改、空格确认
- 每关完成后显示得分/正确率/星级，提供「🔄 再来一次」和「▶ 下一关」按钮；通关第10关显示 🏆 恭喜全部通关

## 2026-07-29

### ✅ 修复 Supabase 数据不同步问题
- **根因**：`loadSystemData()` 中 Supabase 查询成功后直接 `saveSystemData()`，用空数据覆盖了 localStorage 中的成绩和文章
- **修复**：先从 localStorage 加载数据作为备份；Supabase 返回空数组时保留本地数据；Supabase 加载失败时完整回退到 localStorage
- 为所有 Supabase 写入操作（学生增删改、文章增删、成绩保存、密码修改等）添加 `console.error` 错误日志，写入失败时弹出 toast 提示

### ✅ 新增「同步到云端」手动同步按钮
- 教师后台 → 系统设置 → 数据管理区新增 **☁️ 同步到云端** 按钮
- 一键将本地所有数据（学生、文章、成绩、设置）强制推送到 Supabase
- 先清空云端旧数据再写入，每步都有错误提示

### ✅ 清理 git 历史中的敏感文件
- 使用 `git filter-branch` 从全部历史中移除 `opencode_restore_package` 目录（含 GitHub Token）
- 强制推送覆盖远程仓库，解决 GitHub 推送保护拦截

## 2026-07-28

### ✅ Supabase 云存储完全部署
- 创建 `Supabase配置信息.md`，完整记录数据库表结构、密钥、项目信息
- 创建项目 `typing-practice`（ID: `ezbsuywvaimjgbuhrubj`）
- 填入正确的 Project URL 和 anon public key
- 在 SQL Editor 执行建表语句，4 张表已创建（students、articles、grades、settings）
- RLS 策略已开启，允许匿名读写

### ✅ 首页标题恢复
- 登录页和首页标题从「在线打字练习」改回「潘庄小学打字练习乐园」
- 去掉副标题「提升打字速度，成为键盘高手」

### ✅ 修复批量添加学生只添加部分的问题
- 原因：逐个调用 Supabase API 添加学生，网络波动中途中断
- 修复：改为先在本地去重过滤，再一次性批量写入云数据库（单次 API 调用）
- 提示优化：显示「成功添加 X 名学生（Y 名已存在已跳过）」

## 2026-07-27

### ✅ 首页重新设计
- **登录页改版**：去掉"潘庄小学"文字和键盘SVG；标题改为"在线打字练习"，新增副标题"提升打字速度，成为键盘高手"；学生登录卡放大为主卡（"开始练习"）；教师登录缩小为底部文字链接
- **首页头部**：去掉键盘SVG和"潘庄小学"文字；标题简化为"打字练习"

### ✅ 教师管理 — 成绩管理（第4个标签页）
- 新增"📊 成绩管理"标签页
- 按班级筛选 + 按姓名搜索学生
- 汇总列表显示：练习次数、平均速度、平均正确率、最高速度
- 点击「详情」查看单个学生的所有练习记录（日期、类型、速度、正确率、星级）
- 支持下载CSV（按当前筛选条件导出）

### ✅ 教师管理 — 学生管理改进
- 新增班级筛选下拉框（按班级过滤学生列表）
- 新增批量删除：全选/反选复选框 + 「批量删除」按钮

### ✅ 成绩数据持久化
- 每次练习完成自动保存成绩到 localStorage（键名 `tpGrades`）
- 导出/导入数据功能已包含成绩数据
- 解决跨设备数据迁移问题：教师可在旧设备导出，新设备导入

### ✅ Supabase 云存储集成
- 新增 `Supabase部署教程.md`，详细记录注册、建表、配置、迁移全流程
- 所有数据改为先读 Supabase，失败后自动降级到 localStorage
- 学生增删改、文章增删、密码修改、成绩记录全部同步写入 Supabase
- 导入数据功能增强：导入时自动覆盖写入 Supabase
- 网站启动时自动从云数据库拉取最新数据
- 字段映射：Js 的 `isDefault` ↔ 数据库的 `is_default`

### ✅ 导出/导入数据增强
- 导出文件名带日期（`typing_practice_data_2026-07-27.json`）
- 导入/导出均包含学生、文章、设置、成绩全部数据

### ✅ 第一批：7个Bug修复
- **空格键页面跳动**：打字和游戏键盘事件开头加 `e.preventDefault()`，阻止浏览器默认滚动行为
- **练习页面不能完全显示**：`.typing-container` 加 `max-height:95vh;overflow-y:auto`
- **赛车键盘无反应**：`gameTarget` 从完整单词改为单词首字母，键盘比对逻辑修复
- **泡泡字母不对应**：泡泡消除后 `nextBubbleTarget()` 移入 `setTimeout(300ms)` 回调，等待动画完成后再更新目标
- **星星显示对应字母**：星星内容从固定 `⭐` 改为显示实际字母（大写）
- **游戏结束关不掉**：HTML 中 `onclick` 从 `closeResultAndGo` 改为 `closeGameOverAndGo`，新增 `closeGameOverAndGo()` 函数正确关闭游戏结束弹窗
- **页面溢出**：同上修复（overflow-y 滚动支持）

### ✅ 第二批：3个游戏优化
- **赛车游戏全面重写**：改用字母表26个随机字母（取代从单词取首字母，更直观）；输入检测兼容中文输入法（IME）模式（`e.key='Process'`/`'Dead'` 时自动回退到 `e.keyCode`）；赛车下方新增可视三排QWERTY键盘，目标字母高亮显示，支持点击触发射击
- **泡泡字母唯一化**：改为从26个字母随机洗牌后取6个不重复字母，彻底解决重复字母导致的目标混淆
- **星星界面显示目标键**：星星区域右上角新增黄色「目标: X」标签，即时指示需要击落的字母

### ❌ 强制重新部署失败（网络问题）
- 用户要求强制上传更新 `index.html` 和 `style.css`
- 创建空提交 `git commit --allow-empty` 触发 GitHub Pages 重新部署
- `git push` 连接 `github.com:443` 超时/连接被重置
- **解决方案**：网络恢复后运行 `git push`，或按 `手动更新GitHub教程.md` 通过网页上传

## 2026-07-25

### ✅ 修复首页登录按钮无响应
- **问题**：扩展练习内容时 `practiceData` 中 `medium:` 和 `hard:` 键名写法错误（冒号进了引号），导致JS解析失败
- **修复**：恢复键名正确写法 `medium:'...'` → `medium:'...'`（冒号在引号外）
- 重新推送至 GitHub 并部署成功

### ✅ 首页全面美化
- 网站标题改为「潘庄小学打字练习乐园」
- 去掉「小学三至五年级专用」字样
- 首页标题改用渐变色文字 + 键盘SVG图标
- 登录页面学生/教师图标改用精美SVG图形
- 首页5个菜单按钮改用纯SVG图标（替代Emoji）
- 触屏练习图标放大到70px，卡片更醒目
- 增加横屏使用提示，自动请求`orientation.lock('landscape')`
- 练习内容文字量增加50%-100%（每个项目）
- 打字区域最大宽度扩展至1100px，更好适配桌面
- 全局圆角统一为24-32px，阴影加大，背景渐变优化
- 所有输入框、按钮、卡片间距加大

### ✅ 音效全覆盖
- 触屏练习增加正确/错误音效反馈
- 原有练习和游戏的音效保持不变

### ✅ 创建更新进度跟踪文件
- 新建 `更新进度日志.md` 自动记录每次更新

---

## 2026-07-24

### ✅ 修复教师登录跨设备bug
- **问题**：一台电脑改了教师密码，另一台电脑还是旧密码导致登录失败
- **修复**：默认教师密码改为 `pzxxzzw`，所有新设备首次加载即可使用
- **加固**：密码输入增加 `.trim()` 去空格处理

### ✅ 班级数据修正
- **三年级** 从2个班扩展为 **6个班**
- **四年级** 从2个班扩展为 **5个班**
- **五年级** 从2个班扩展为 **5个班**
- 修复班级下拉框只显示已存在学生班级的问题，改为始终显示全部16个默认班级

### ✅ GitHub 更新教程
- 新建 `手动更新GitHub教程.md`，记录网页上传操作步骤
- 补充分支选择、Add file按钮位置、常见错误解决方法

---

## 2026-07-23

### ✅ 学生批量添加改进
- **原来**：需用「三年级1班-张三」格式，容易写错班级名
- **现在**：先选班级（下拉框），再直接粘贴学生姓名（每行一个）
- 更直观、不易出错

### ✅ 文章管理 — Word文件上传
- 新增「📄 上传Word文件」按钮
- 支持 .docx 格式，自动提取文字内容
- 标题和正文自动填入输入框，教师可预览修改后再添加
- 使用 mammoth.js 库解析（从 CDN 加载）

### ✅ 项目初始化
- 搭建完整打字练习网站（index.html + style.css + app.js）
- 学生登录系统（选择班级 + 姓名 + 密码）
- 教师后台管理系统（学生管理、文章管理、系统设置）
- 打字练习功能（8种练习类型 + 3种游戏）
- 成绩追踪与成就系统
- 数据持久化（localStorage）

---

## 待办 / 规划中

- [ ] 学生练习记录统计图表
- [ ] 打字比赛模式（多人竞技）
- [ ] 词库管理功能（教师自定义词库）
- [ ] 离线 PWA 支持

---

## 文件结构

```
F:\opencode\wangzhan\
├── index.html                  ← 网站首页（全部页面结构）
├── style.css                   ← 样式文件（全站风格）
├── app.js                      ← 功能逻辑（全部JS代码）
├── 更新进度日志.md              ← 本文件
├── 手动更新GitHub教程.md         ← 网页上传操作指南
├── GitHub更新教程.md             ← Git命令行操作指南
└── GitHub部署教程.md             ← 首次部署指南
```

## 访问地址

```
仓库:  https://github.com/vincent-az/typing-practice
网站:  https://vincent-az.github.io/typing-practice/
```
